<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;
]>

<sect1 id="aboutgl" xreflabel="About OpenGL">
  <?dbhtml filename="aboutgl.html"?>

  <title>About OpenGL</title>

  <para>
    OpenGL is an older high-level graphics API which most graphical packages on
    Linux use today.
  </para>

  <para>
    In BLFS, OpenGL libraries (<filename class="libraryfile">libGL</filename>
    and others) are installed from Mesa. This is contrasted with multiple
    Linux distributions installing those libraries from <xref
    linkend="libglvnd"/>. GLFS, unlike BLFS, has made the same decision as
    those distributions and expects you to install <xref
    linkend="libglvnd"/>. Despite this, <xref linkend="mesa"/> is required for
    a lot of systems because it provides necessary drivers, not just OpenGL
    libraries. GLFS makes the instructions in that package request libglvnd
    support so that Mesa does not build its own OpenGL libraries.
  </para>

  <note><para>
    In BLFS, the packages do support libglvnd, but BLFS makes no mention of it.
    Whenever BLFS says a package depends on Mesa, it means that package will
    need OpenGL libraries (<xref linkend="libglvnd"/>) and/or libgbm (<xref
    linkend="libgbm"/> or libgbm from <xref linkend="mesa"/>). Certain pages
    will also link to GLU. However, you must install this book's version of
    <xref linkend="glu"/> to ensure compatibility with libglvnd.
  </para></note>

  <para>
    If you haven't installed any packages relying on OpenGL yet, go ahead and
    install <xref linkend="libglvnd"/> and the driver for your GPU. If you
    have installed Mesa from BLFS, you probably have the OpenGL libraries
    provided by Mesa. The main issues you will come across by transtioning are
    soname differences in <filename class="libraryfile">libGL</filename> as
    well as potential ABI issues. As a result, many packages will require
    reinstallation in order to use the right libraries. For that reason,
    transitioning isn't recommended and it might be better to reinstall LFS
    and its subprojects with libglvnd in mind.
  </para>

  <para>
    While you can install most of the packages in GLFS with Mesa's OpenGL
    libraries, and even enable the installation of those libraries from Mesa in
    this book by removing a documented option, it is strongly recommended that
    you don't. GLFS nor SLFS support that configuration.
  </para>

  <para>
    There is more information regarding OpenGL and libglvnd. If you just want
    to go through the book as intended, feel free to skip to installing
    libglvnd.
  </para>

  <sect2>
    <title>Why Does GLFS Install libglvnd</title>

    <para>
      The primary reason GLFS expects users to install libglvnd is that <xref
      linkend="nvidia"/> and several packages and AppImages require libglvnd
      either directly or indirectly. For NVIDIA, it requires libglvnd outright,
      while AppImages suffer from the problem mentioned earlier in this
      section: differences in sonames, as well as a missing library. <filename
      class="libraryfile">libGL</filename> has a soname that's different from
      Mesa's and <filename class="libraryfile">libGLX</filename> is not
      included from Mesa. This breaks most AppImages. As for other packages,
      their build systems can cause configuration to fail without libglvnd as
      <filename class="libraryfile">libOpenGL</filename> doesn't exist, and the
      build systems don't want to combine <filename
      class="libraryfile">libGL</filename> with <filename
      class="libraryfile">libEGL</filename>.
    </para>

    <para>
      These issues leak into Supplemental LFS, like with Hyprland and
      OBS-Studio. Flatpak may suffer from the same issue. BLFS may suffer from
      them at some point, depending on when projects make changes that require
      libglvnd in some form.
    </para>

  </sect2>

  <sect2>
    <title>Will BLFS Provide libglvnd</title>

    <para>
      With the amount of packages beginning to require libglvnd growing in
      number, it's a matter of time before BLFS will have to adopt libglvnd.
      There have been efforts to get libglvnd into BLFS sooner rather than
      later to avoid future headaches. The most significant push can be found
      in this <ulink
      url="https://lists.linuxfromscratch.org/sympa/arc/blfs-dev/2025-09/msg00002.html">thread</ulink>.
      The thread mentions that Mesa supplies <filename
      class="libraryfile">libGLX</filename>, this is wrong. Either way, the
      request was shot down. Until BLFS has a package that requires libglvnd,
      it probably won't move away from Mesa's OpenGL libraries.
    </para>

  </sect2>

</sect1>
