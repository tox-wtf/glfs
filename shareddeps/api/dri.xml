<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;

  <!ENTITY mesa-download
    "https://mesa.freedesktop.org/archive/mesa-&mesa-version;.tar.xz">
  <!ENTITY mesa-raw-repo
    "https://gitlab.freedesktop.org/mesa/mesa/-/raw/&mesa-major-minor;">
]>

<sect1 id="dri" xreflabel="DRI from Mesa-&mesa-version;">
  <?dbhtml filename="dri.html"?>

  <title>DRI from Mesa-&mesa-version;</title>

  <indexterm zone="dri">
    <primary sortas="a-DRI">DRI</primary>
  </indexterm>

  <sect2 role="package">
    <title>Introduction to DRI</title>

    <para>
      DRI, or Direct Rendering Infrastructure, is a framework that gives direct
      access to hardware capable of generating graphics for X.org. While
      Wayland does not use userland DRI, Xwayland that is reponsible for giving
      X.org application support to Wayland does want the DRI interface. While
      Xorg-Server and Xwayland want the interface, it doesn't strictly need it
      if the code for both packages were slightly altered.
    </para>

    <para>
      Nonetheless, installing the DRI interface is a good idea. DRI comes from
      Mesa, and there's a few ways to install the interface and necessary
      headers. You could download the headers, and skip downloading the Mesa
      tarball. If you already have the tarball downloaded, then installing the
      files from it may be a better idea.
    </para>

    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Download: <ulink url="&mesa-download;"/>
        </para>
      </listitem>
    </itemizedlist>

    <bridgehead renderas="sect3">DRI Dependencies</bridgehead>

    <para role="recommended">
      <xref linkend="wget"/> or <xref linkend="curl"/> (if downloading the
      files instead of the tarball)
    </para>

  </sect2>

  <sect2 role="installation">
    <title>Installation of DRI from the Mesa Tarball</title>

    <para>
      If you have chosen to install the files from the Mesa tarball, extract it
      and go into the extracted directory as normal.
    </para>

    <para>
      After that, install the headers for the interface, as well as some other
      expected headers, as the &root; user:
    </para>

<screen role="root"><userinput>&install-file; include/GL/internal/dri_interface.h \
        -t      /usr/include/GL/internal/          &amp;&amp;
&install-file; include/EGL/eglmesaext.h            \
        -t      /usr/include/EGL/                  &amp;&amp;
&install-file; include/EGL/eglext_angle.h          \
        -t      /usr/include/EGL/</userinput></screen>

    <para>
      Now that interface has been installed, you need the pkg-config file
      showing that the interface is present. Create it now as the &root; user:
    </para>

<screen role="root"><userinput>cat &gt; dri.pc &lt;&lt; "EOF"
<literal>prefix=/usr
includedir=${prefix}/include

dridriverdir=${prefix}/lib/dri

Name: dri
Description: Direct Rendering Infrastructure
Version: MESA_VERSION
Requires.private: libdrm >=  2.4.109
Cflags: -I${includedir}</literal>
EOF

sed -i "s/MESA_VERSION/`cat VERSION`/" dri.pc &amp;&amp;
&install-file; dri.pc -t /usr/lib/pkgconfig/ &amp;&amp;
rm -vf dri.pc</userinput></screen>

    <para>
      All the files have now been installed.
    </para>

  </sect2>

  <sect2 role="installation">
    <title>Downloading DRI from Mesa</title>

    <para>
      If you opted to downloaded the DRI interface from the internet instead,
      then there is no setup needed before downloading the interface besides
      ensuring that you have an internet connection, installed <xref
      linkend="make-ca"/>, and ensured <xref linkend="wget"/> is operational.
      <xref linkend="curl"/> can be used instead of Wget if you wish.
    </para>

    <para>
      The files that will be downloaded come directly from the
      GitLab/FreeDesktop repository for Mesa from a branch or tag that has the
      same major and minor version of Mesa that's currently in this book.
    </para>

    <para>
      Now go ahead and download the interface and other necessary headers as
      the &root; user:
    </para>

<screen role="root"><userinput>&install-dir; /usr/include/GL/internal /usr/include/EGL &amp;&amp;
wget &mesa-raw-repo;/include/GL/internal/dri_interface.h \
  -O /usr/include/GL/internal/dri_interface.h &amp;&amp;
wget &mesa-raw-repo;/include/EGL/eglmesaext.h \
  -O /usr/include/EGL/eglmesaext.h &amp;&amp;
wget &mesa-raw-repo;/include/EGL/eglext_angle.h \
  -O /usr/include/EGL/eglext_angle.h</userinput></screen>

    <para>
      Now that the interface has been installed, you need the pkg-config file
      saying that it's present on the system. Create it now as the &root; user:
    </para>

<screen role="root"><userinput>cat &gt; /usr/lib/pkgconfig/dri.pc &lt;&lt; "EOF"
<literal>prefix=/usr
includedir=${prefix}/include

dridriverdir=${prefix}/lib/dri

Name: dri
Description: Direct Rendering Infrastructure
Version: &mesa-version;
Requires.private: libdrm >=  2.4.109
Cflags: -I${includedir}</literal>
EOF</userinput></screen>

    <para>
      Every file has now been downloaded and installed.
    </para>

  </sect2>

  <sect2 role="content">
    <title>Contents</title>
    <segmentedlist>
      <segtitle>Installed Programs</segtitle>
      <segtitle>Installed Libraries</segtitle>
      <segtitle>Installed Directories</segtitle>
      <seglistitem>
        <seg>
          None
        </seg>
        <seg>
          None
        </seg>
        <seg>
          /usr/include/GL/internal and /usr/include/EGL
        </seg>
      </seglistitem>
    </segmentedlist>
  </sect2>

</sect1>
