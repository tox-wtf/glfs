<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;

  <!ENTITY sdl2-download "https://www.libsdl.org/release/sdl2-compat-&sdl2-version;.tar.gz">
]>

<sect1 id="sdl2" xreflabel="sdl2-compat-&sdl2-version;">
  <?dbhtml filename="sdl2.html"?>

  <title>sdl2-compat-&sdl2-version;</title>

  <indexterm zone="sdl2">
    <primary sortas="a-SDL2">sdl2-compat</primary>
  </indexterm>

  <sect2 role="package">
    <title>Introduction to sdl2-compat</title>

    <para>
      The sdl2-compat package provides a compatibility layer for packages
      that require SDL2, utilizing SDL3 as the backend.
    </para>

    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Download (HTTP): <ulink url="&sdl2-download;"/>
        </para>
      </listitem>
    </itemizedlist>

    <bridgehead renderas="sect3">sdl2-compat Dependencies</bridgehead>

    <para role="required">
      <xref linkend="cmake"/> and
      <xref linkend="sdl3"/>
    </para>

  </sect2>

  <sect2 role="installation">
    <title>Installation of sdl2-compat</title>

    <para>
      Install sdl2-compat by running the following
      commands:
    </para>

<screen><userinput>mkdir build &amp;&amp;
cd    build &amp;&amp;

cmake -D CMAKE_INSTALL_PREFIX=/usr   \
      -D CMAKE_BUILD_TYPE=Release    \
      -D CMAKE_SKIP_INSTALL_RPATH=ON \
      -D SDL2COMPAT_STATIC=OFF       \
      -D SDL2COMPAT_TESTS=OFF        \
      -W no-author -G Ninja ..      &amp;&amp;

ninja</userinput></screen>

    <para>
      Now, as the &root; user:
    </para>

<screen role="root"><userinput>ninja install &amp;&amp;
rm -vf /usr/lib/libSDL2_test.a</userinput></screen>

  </sect2>

  <sect2 role="installation">
    <title>lib32 Installation of sdl2-compat</title>

    <para>
      Install lib32-sdl2-compat by running the
      following commands:
    </para>

<screen><userinput>rm -rf * &amp;&amp;
CC="gcc -m32" CXX="g++ -m32"         \
PKG_CONFIG_PATH=/usr/lib32/pkgconfig \
cmake -D CMAKE_INSTALL_PREFIX=/usr   \
      -D CMAKE_INSTALL_LIBDIR=lib32  \
      -D CMAKE_BUILD_TYPE=Release    \
      -D CMAKE_SKIP_INSTALL_RPATH=ON \
      -D SDL2COMPAT_STATIC=OFF       \
      -D SDL2COMPAT_TESTS=OFF        \
      -W no-author -G Ninja ..      &amp;&amp;

ninja</userinput></screen>

    <para>
      Now, as the &root; user:
    </para>

<screen role="root"><userinput>DESTDIR=$PWD/DESTDIR ninja install      &amp;&amp;
rm -vf DESTDIR/usr/lib32/libSDL2_test.a &amp;&amp;
cp -vr DESTDIR/usr/lib32/* /usr/lib32   &amp;&amp;
rm -rf DESTDIR                          &amp;&amp;
ldconfig</userinput></screen>

  </sect2>

  <sect2 role="commands">
    <title>Command Explanations</title>

    &options-cmake;

    &cmake-skip-install-rpath;

    <para>
      <parameter>-D SDL2COMPAT_STATIC=OFF</parameter>: This parameter disables
      building the static libraries.
    </para>

    <para>
      <parameter>-D SDL2COMPAT_TESTS=OFF</parameter>: This parameter disables
      building the tests.
    </para>

    <para>
      <command>rm -vf /usr/lib/libSDL2_test.a</command>: This command removes a
      useless static library.
    </para>

  </sect2>

  <sect2 role="content">
    <title>Contents</title>

    <segmentedlist>
      <segtitle>Installed Program</segtitle>
      <segtitle>Installed Libraries</segtitle>
      <segtitle>Installed Directories</segtitle>

      <seglistitem>
        <seg>sdl2-config</seg>
        <seg>libSDL2</seg>
        <seg>/usr/include/SDL2 and
             /usr/lib/cmake/SDL2</seg>
      </seglistitem>
    </segmentedlist>

    <variablelist>
      <bridgehead renderas="sect3">Short Descriptions</bridgehead>
      <?dbfo list-presentation="list"?>
      <?dbhtml list-presentation="table"?>

      <varlistentry id="sdl2-config">
        <term><command>sdl2-config</command></term>
        <listitem>
          <para>
            determines the compile and linker flags that should be used to
            compile and link programs that use
            <filename class="libraryfile">libSDL2</filename>
          </para>
          <indexterm zone="sdl2 sdl2-config">
            <primary sortas="b-sdl2-config">sdl2-config</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="libsdl2">
        <term><filename class="libraryfile">libSDL2</filename></term>
        <listitem>
          <para>
            provides compatibility functions for SDL2 applications using SDL3
          </para>
          <indexterm zone="sdl2 libsdl2">
            <primary sortas="c-libsdl2">libSDL2</primary>
          </indexterm>
        </listitem>
      </varlistentry>

    </variablelist>

  </sect2>

</sect1>
