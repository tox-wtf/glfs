<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;
]>

<sect1 id="winedeps" xreflabel="Wine Dependencies">
  <?dbhtml filename="winedeps.html"?>


  <title>Wine Dependencies</title>

  <para>
    Wine has four build formats and depending on what build you want,
    you will have to pay attention to what you install and its format.
  </para>

  <para>
    The four build types are: WoW64, old WoW64, 64-bit, and 32-bit. 32-bit and
    64-bit are self-explanatory, but both WoW64 types need explanation. WoW64
    is a compatibility layer created by Microsoft when applications and
    computers were becoming 64-bit. This layer allows users to emulate 32-bit
    Windows applications on a 64-bit CPU. For Wine, this has the same premise;
    it allows you to run both 32-bit and 64-bit Windows apps and games on
    x86_64. However, there are two WoW64 types.
  </para>

  <para>
    The first one is the newer and default WoW64 architecture. All you need are
    64-bit libraries and both the x86_64 and i686 MinGW-w64 toolchains. The
    older architecture uses 32-bit libraries to bring the 32-bit support. It is
    in opposition to a goal the desktop has been moving towards: full 64-bit,
    leaving 32-bit libraries behind. So the new architecture was developed to
    satisfy it.
  </para>

  <para>
    The WoW64 option can be a good choice, and if you go with that route, you
    won't need MLFS at all, or any 32-bit libraries from this book, unless you
    want or already have installed Steam. With that, you only need to do the
    normal installations of each package, and for the MinGW-w64 toolchain, you
    still have to install both the x86_64 and i686 toolchains.
  </para>

  <para>
    If you have an issue with the newer WoW64 architecture or trust the older
    system more, you will then need both the 64-bit and lib32 variants of each
    package, along with the x86_64 and i686 MinGW-w64 toolchains.
  </para>

  <para>
    For pure 64-bit, follow the instructions normally and skip the lib32
    instructions. When you install the MinGW-w64 toolchain, you can just follow
    the x86_64 instructions.
  </para>

  <para>
    For pure 32-bit, follow only the lib32 instructions. When you reach
    MinGW-w64, follow the i686 instructions. However, for the sake of
    continuing onto BLFS, it is recommended to do the normal installations as
    well, save for MinGW-w64. Along with that, Wine may need programs from the
    given set of packages and this installation method is not tested without
    the normal 64-bit installations.
  </para>

  <para role="required">
    Audio (<xref linkend="alsa-plugins"/> and/or
    <xref linkend="pulseaudio"/>), <xref linkend="dbus"/>,
    Display server (<xref linkend="xorg-server"/> or
    <xref linkend="wayland"/> w/ <xref linkend="xwayland"/>),
    <xref linkend="fontconfig"/>,
    <xref linkend="freetype2"/>,
    <xref linkend="gnutls"/>,
    <xref linkend="libunwind"/> (if 64-bit/WoW64; not used by Wine 32-bit),
    &opengl;, and
    <xref linkend="mingw-w64-gcc"/>
  </para>

  <para role="recommended"><itemizedlist spacing="compact">
    <listitem>
      <para>
        <xref linkend="gstreamer"/>: Allows consuming and working with
        multimedia such as music and videos. Most games and some software
        work without it, although some will need it for WMA decoding.
      </para>
    </listitem>
    <listitem>
      <para>
        &sdl2;: For input handling and other features.
      </para>
    </listitem>
    <listitem>
      <para>
        <xref linkend="vulkan-sdk"/>: For Vulkan support.
      </para>
    </listitem>
  </itemizedlist></para>

  <para role="optional">
    <ulink url="https://aur.archlinux.org/packages/libcapi/">
    libcapi</ulink>,
    <ulink url="&blfs-svn;/pst/cups.html">Cups</ulink>,
    <ulink url="https://archlinux.org/packages/extra/x86_64/gphoto2/">
    gphoto2</ulink>,
    <ulink url="&blfs-svn;/basicnet/samba.html">Samba</ulink>,
    <ulink url="&blfs-svn;/pst/sane.html">SANE</ulink>,
    <ulink url="&blfs-svn;/postlfs/mitkrb.html">Kerberos</ulink>,
    <ulink url="https://archlinux.org/packages/extra/x86_64/smbclient/">
    smbclient</ulink>,
    <xref linkend="opencl-sdk"/>,
    <ulink url="&blfs-svn;/basicnet/libpcap.html">libpcap</ulink>,
    <ulink url="&blfs-svn;/general/libusb.html">libusb</ulink>, and
    <ulink url="&blfs-svn;/multimedia/v4l-utils.html">v4l-utils</ulink>
  </para>

</sect1>
